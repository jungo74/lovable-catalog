// JSON-LD est maintenant entièrement géré en statique dans index.html
// Ce fichier est conservé pour compatibilité mais n'exporte rien de dynamique

// Les composants suivants sont supprimés car le JSON-LD doit être statique :
// - OrganizationJsonLd (déplacé dans index.html)
// - ProductJsonLd (supprimé - pas de structured data Product en SPA catalogue)
// - BreadcrumbJsonLd (supprimé)
// - WebSiteJsonLd (supprimé)

export {};
